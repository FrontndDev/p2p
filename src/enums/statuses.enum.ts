export enum StatusesEnum {
  'in-process' = 'В процессе',
  'completed' = 'Завершена',
  'canceled' = 'Отменена',
}

export enum SecondStatusesEnum {
  'in-process' = 'in-process',
  'completed' = 'completed',
  'canceled' = 'canceled',
}